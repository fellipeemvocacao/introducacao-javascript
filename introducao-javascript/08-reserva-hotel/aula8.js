//Aritmética + Comparação + Ternário//

const nomeHospede = "Ana"
const valorDiaria = 180;
const noites = 5.
const temDesconto = temDesconto ? 0.9 : 1;
const totalReserva = valorDiaria * noites * fatorDesconto;

console.log(totalReserva);
document.getElementById("resultado").textContent = `Reserva de ${noites} - total R$${totalReserva.toFixed(2)}`;
